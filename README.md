# Iniciar flask

```bash
flask --app hello --debug run
```

# Desactivar proxy

```
unset http_proxy
unset https_proxy
unset HTTP_PROXY
unset HTTPS_PROXY
```

# Iniciar python

```bash
source venv/bin/activate
python3 cli.py
```